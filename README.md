Ensure you have the dataset (fake_ecommerce_data.csv).
2️⃣ Install the required libraries:

pip install pandas numpy matplotlib seaborn plotly streamlit scikit-learn

streamlit run customer_analysis.py
